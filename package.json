{
  "version": "0.3.0",
  "private": true,
  "engines": {
    "yarn": "^1.10.1"
  },
  "scripts": {
    "build": "rm -rf assets build static *.html && yarn vuepress build src --dest build && cp -rf build/ .",
    "check": "echo \"no checks\"",
    "clean": "echo \"no clean\"",
    "start": "yarn vuepress dev src",
    "test": "echo \"no test\""
  },
  "devDependencies": {
    "vuepress": "^1.0.2"
  }
}
